interface:
  display_name: "Tasklist Review"
  short_description: "Review tasks.md for autonomous execution"
  default_prompt: "Use $tasklist-review to review tasks.md and rewrite blocking tasks so an agent can execute without stopping."
